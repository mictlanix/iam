Dependencies

 1. Install NuGet Command Line
    Download it from http://nuget.codeplex.com/releases
 2. Make a Packages directory
    $ mkdir Packages
 3. Download libraries using nuget
    $ nuget install Mictlanix.Iam/packages.config -o Packages

